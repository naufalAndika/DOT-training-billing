## Build Setup
copy .env.example to.env and edit .env as we want
``` bash
$ cp .env.example .env
```

``` bash
# install packages
$ composer install

# run migration
$ php artisan migrate

```

## Running Server
``` bash
$ php artisan serve
```
